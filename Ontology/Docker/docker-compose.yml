services:
  age:
    image: apache/age
    container_name: age_graphdb
    ports:
      - "5455:5432"
    environment:
      POSTGRES_USER: ageuser
      POSTGRES_PASSWORD: KIM10507
      POSTGRES_DB: ontology
    volumes:
      - age_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  age_data:
